# .streamlit/secrets.toml.template
# Rename this file to 'secrets.toml' and replace the placeholders with your actual API keys.
# Do NOT commit 'secrets.toml' to git (it is ignored by .gitignore).

MONDAY_API_TOKEN = "your_monday_api_token_here"
GEMINI_API_KEY = "your_gemini_api_key_here"
